name: TJSV resident contract consumer
on:
  pull_request:
  push:
    branches: [main, dev]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  resident-contract-admission:
    uses: hacker-house-medellin/hhm-interfaces/.github/workflows/reusable-resident-contract-admission.yml@7116e3a3cb239b5e07ba7501c4c7ef6736fdb57f
    with:
      consumer_repository: ${{ github.repository }}
      consumer_role: hhm-domain-and-admin-queue
# DEN-1950 / DEN-3828. The authority-owned workflow pins the independent
# TypeSpec and authored JSON Schema sources and ORESoftware/typespec-json-schema-validator,
# reproduces fresh Contract IR, and proves current-input admission and seven refusals.
# Runtime lifecycle, money, legal, PostgreSQL, and admin-queue behavior remains
# covered separately by native tests and the executable formal-methods gates.
